{
  "name": "assignment-12",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:server": "vite",
    "sass": "sass src/scss/main.scss src/styles/site.css --style=expanded --no-source-map",
    "sass:watch": "sass --watch src/scss/main.scss src/styles/site.css --style=expanded --no-source-map",
    "build": "node scripts/build.mjs",
    "build:site": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.99.0",
    "vite": "^8.0.4"
  }
}
